@model IEnumerable<WebProject.Models.Substructure>

<div class="substructure-lab-wrapper">
    <div class="substructure-lab">
        <div class="substructure-lab-title">
            <h1>Altyapı</h1>
        </div>
        <div class="content-block">
            @foreach (var item in Model)
            {
                <div class="card bg-gradient">
                    <img src="@item.PicturePath" alt="@item.Name resim" class="card-img">
                    <div class="card-body">
                        <h4 class="card-title">
                            <a asp-controller="Home" asp-action="substructureDetail" asp-route-id="@item.Id">@item.Name</a>
                        </h4>
                        <p class="card-text">@item.Description</p>
                    </div>
                </div>
            }
        </div>
    </div>
</div>